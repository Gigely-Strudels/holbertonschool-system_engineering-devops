#!/usr/bin/env bash
# This script installs nginx web server on a server
sudo apt-get update
sudo apt-get install -y nginx
curl -sI localhost:80 | grep "200 OK"
curl -sS http://<server-ip-address>/ | grep "Hello World"
